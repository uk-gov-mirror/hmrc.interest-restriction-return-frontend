{% from "govuk/components/radios/macro.njk"        import govukRadios %}

{{ govukRadios({
    name: 'value',
    fieldset: {
        legend: {
            text: messages(headingKey),
            isPageHeading: true,
            classes: "govuk-fieldset__legend--xl"
        }
    },
    items: radios,
    errorMessage: form.value.error
}) }}